# Synced schemas

Run `pnpm schema:sync` to populate this directory with the current Compose JSON schemas and the supplied documentation snapshots. Generated files are intentionally not committed by the initial scaffold.
